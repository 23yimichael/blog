mutation updateArticle(
  $id: Int!
  $title: String!
  $genre: String!
  $img: String!
  $text: String!
) {
  updateArticle(id: $id, title: $title, genre: $genre, img: $img, text: $text) {
    error {
      field
      message
    }
    article {
      id
    }
  }
}
